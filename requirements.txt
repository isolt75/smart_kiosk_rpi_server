# === Hardware Control (Raspberry Pi OS only) ===
gpiozero==2.0.1
RPi.GPIO==0.7.1
picamera2==0.3.19

# === Audio Processing ===
sounddevice==0.4.7
pyaudio==0.2.14
webrtcvad==2.0.10
numpy==1.26.4

# === Network Communication ===
requests==2.32.3
urllib3==2.2.2
requests-toolbelt==1.0.0

# === Concurrency & Utilities ===
python-dotenv==1.0.1
pydantic==2.7.4
pydantic-settings==2.3.4
APScheduler==3.10.4
tenacity==8.4.2

# === Logging & Monitoring ===
loguru==0.7.2
psutil==5.9.8

# === Imaging (picamera2 출력 → JPEG 인코딩) ===
Pillow==10.4.0

# === Plate Detection (YOLOv8) ===
# Pi 4에서 PyTorch 2.x 휠 설치는 무겁다. ONNX 변환 모델 사용 시 ultralytics는
# onnxruntime 백엔드로 동작한다. 모델 파일은 별도 배치(.env로 경로 지정).
ultralytics==8.2.50
opencv-python-headless==4.10.0.84
onnxruntime==1.18.1

# === Development ===
pytest==8.2.2
pytest-mock==3.14.0
black==24.4.2
